docker
exec
exit
ls
pwd
node index.js
exit
ls
if [ -f ${index.html} ]; then
echo "ファイルが存在します"
else
echo "ファイルが存在しません"
fi
if [[ -f ${index.html} ]]; then
echo "ファイルが存在します"
else
echo "ファイルが存在しません"
fi
if [[ -f ${index.html} ]]; then
echo "ファイルが存在しません"
fi
if [ 1 > 0 ]; then
echo "正解"
fi
FILE="index.js"
if [ -f ${FILE} ]; then
echo "ファイルが存在します"
fi
if [ -f ${FILE} ]; then
echo "ファイルが存在します"
fi
if [ -f ${FILE} ]; then
echo "ファイルが存在します"
else
echo "ファイルが存在しません"
fi
FILE="index4.html"
if [ -f ${FILE} ]; then
echo "ファイルが存在します"
else
echo "ファイルが存在しません"
fi
